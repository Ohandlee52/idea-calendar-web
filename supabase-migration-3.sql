-- =============================================================
--  아이디어 캘린더 - 추가 SQL (3번째): 실시간 감지 켜기
--  실행 방법: Supabase -> SQL Editor -> New query -> 붙여넣고 Run
--
--  왜 필요한가요?
--   한쪽 기기에서 메모를 바꾸면 다른 기기가 "바로" 알아채도록 하는 기능입니다.
--   이걸 켜지 않아도 1분마다 자동으로 확인하므로 동작은 하지만,
--   켜두면 몇 초 안에 반영됩니다.
-- =============================================================

alter publication supabase_realtime add table public.idea_memos;

-- 이미 켜져 있으면 "already member of publication" 오류가 납니다.
-- 그 오류는 무시하셔도 됩니다 (이미 켜져 있다는 뜻).
